import test from 'node:test';import assert from 'node:assert/strict';import{normalizeOrchestration}from'./orchestrator.mjs';test('sparse orchestration drops invalid and unused actions',()=>{const out=normalizeOrchestration({t:'你好',e:'happy',g:[['wave','r',3],['bad','l',1]],v:['bright',.5]});assert.equal(out.t,'你好');assert.deepEqual(out.g,[['wave','r',1]]);assert.deepEqual(out.v,['bright',.5]);});
