NIVA 0.99.1 · Predictive Stability V2 + Joint Rotation Safety Guard
CharacterFrame is the only source of forward/right/up (+Z/+X/+Y); FacingController owns root yaw; Physics owns Rapier/root translation/ground contacts/COM + Capture Point prediction/recovery-step planning; IK owns post-animation limb correction; JointRotationGuard is the mandatory final per-frame humanoid quaternion safety layer before VRM update, enforcing anatomical envelopes, soft-limit compression and angular-speed caps; whole-body stability and deterministic breathing + heartbeat remain additive layers.
